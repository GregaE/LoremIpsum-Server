import express from 'express';
// import db from 'db';

const router = express.Router();

// router.get("", );
// router.post("", );
// router.put("", );
// router.delete("", );

export default router;
